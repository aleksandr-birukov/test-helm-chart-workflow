apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-test-in-customdir.fullname" . }}
  labels:
    {{- include "chart-test-in-customdir.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chart-test-in-customdir.selectorLabels" . | nindent 4 }}
